import { SplitAnswerPipe } from './split-answer.pipe';

describe('SplitAnswerPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitAnswerPipe();
    expect(pipe).toBeTruthy();
  });
});
